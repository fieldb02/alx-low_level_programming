#include <stdio.h>
#include <stdlib.h>
#include <string.h>

/**
 * main - Prints all letters except q and e
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char alphabet;

	alphabet = 'a'
	if (alphabet == 'e' & 'q')
	{
		putchar(alphabet);
		alphabet++;
	}
	putchar('\n');
	return (0);
}
