#include "main.h"
#include <stdio.h>

/**
 * add - adds two integers and returns the result
 *
 * @i: integer to be added
 *
 * @j: integer to be added
 *
 * Return: the result
 */
int add(int i, int j)
{
	int x = i + j;

	return (x);
}
